% $Id: Segment_2.tex,v 1.3 1996/07/08 12:19:15 fabri Exp fabri $

\begin {classtemplate} {CGAL_Segment_2<R>}
\CCsection{2D Segment}

\definition  An object $s$ of the data type \classname\ is a directed
straight line segment in the two-dimensional plane $\E_2$, i.e.\ a
straight line segment $[p,q]$ connecting two points $p,q$ 
\LatexHtml{$\in$}{in}
$\R^2$. The segment is topologically closed, i.e.\  the end
points belong to it. Point $p$ is called the {\em source} and $q$
is called the {\em target} of $s$. The length of $s$ is the
Euclidean distance between $p$ and $q$. Note that there is only a function
to compute the square of the length, because otherwise we had to
perform a square root operation which is not defined for all
number types, which is expensive and not exact.

\creation
\creationvariable{s}

\CCstyle{#include <CGAL/Segment_2.h>}

\hidden \constructor{CGAL_Segment_2();}
             {introduces an uninitialized variable \var.}

\hidden \constructor{CGAL_Segment_2(const CGAL_Segment_2<R> &q);}
 	    {copy constructor.}

\constructor{CGAL_Segment_2(const CGAL_Point_2<R> &p, const CGAL_Point_2<R> &q);}
            {introduces a segment \var\ with source $p$
             and target $q$. The segment is directed from the source towards
             the target.}


\operations
\threecolumns{5cm}{4cm}

\hidden \method{CGAL_Segment_2<R> & operator=(const CGAL_Segment_2<R> &q);}
        {Assignment.}

\method{bool operator==(const CGAL_Segment_2<R> &q) const;}
       {Test for equality: Two segments are equal, iff their sources and
        targets are equal.}

\method{bool operator!=(const CGAL_Segment_2<R> &q) const;}
       {Test for inequality.}


\method{CGAL_Point_2<R> source() const;}
       {returns the source of \var.}

\method{CGAL_Point_2<R> target() const;}
       {returns the target of \var.}

\method{CGAL_Point_2<R> min() const;}
       {returns the point of \var\ with lexicographically smallest coordinate.}

\method{CGAL_Point_2<R> max() const;}
       {returns the point of \var\ with lexicographically largest coordinate.}


\method{CGAL_Point_2<R> vertex(int i) const;}
       {returns source or target   of \var:   \CCstyle{vertex(0)} returns
        the source of \var, \CCstyle{vertex(1)} returns the target of \var. 
        The parameter \CCstyle{i} is taken modulo 2, which gives 
        easy access to the other vertex. }

\method{CGAL_Point_2<R> point(int i) const;}
	{returns \CCstyle{vertex(i)}.}

\method{CGAL_Point_2<R> operator[](int i) const;}
       {returns \CCstyle{vertex(i)}.}

\method{R::FT squared_length() const;}
       {returns the squared length of \var. }

\method{CGAL_Direction_2<R> direction() const;}
       {returns the direction from source to target of \var.}


\method{CGAL_Segment_2<R> opposite() const; }
       {returns a segment with source and target point interchanged.}

\method{CGAL_Line_2<R> supporting_line() const;}
       {returns the line $l$ passing through \var. Line $l$  has the
same orientation as segment \var.}

\method{bool is_degenerate() const;}
       {segment \var\ is degenerate, if source and target are equal.}

\method{bool is_horizontal() const;}
       {}

\method{bool is_vertical() const;}
       {}

\method{bool has_on(const CGAL_Point_2<R> &p) const;}
       {A point is on \var, iff it is equal to the source or target 
        of \var, or if it is in the interior of \var.}

\method{bool collinear_has_on(const CGAL_Point_2<R> &p) const;}
       {checks if point $p$ is on segment~\var. This function is faster
        than function \CCstyle{has_on()}.
        \precond $p$ is on the supporting line of \var.}

\method{CGAL_Bbox_2 bbox() const;}
       {returns a bounding box containing~\var.}

\method{CGAL_Segment_2<R>  transform(const CGAL_Aff_transformation_2<R> &t) const;}
       {returns the segment obtained by applying $t$ on the source
        and the target of \var.}

\implementation
A segment is internally represented by two points. 


\end {classtemplate} 

% $Log: Segment_2.tex,v $
% Revision 1.3  1996/07/08 12:19:15  fabri
% *** empty log message ***
%
% Revision 1.2  1996/03/13 15:42:07  fabri
% *** empty log message ***
%
% Revision 1.1  1995/10/19 18:22:12  fabri
% Initial revision
%
